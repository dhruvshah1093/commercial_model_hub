{
    "analyze_invoice": {
        "description": "Extract key fields (invoice number, date, total amount) from the uploaded invoice file.",
        "model": "gpt-4.1",
        "temperature": 0.1,
        "endpoint": "responses"
    }
}